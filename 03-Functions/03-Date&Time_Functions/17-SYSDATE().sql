/*
	SYSDATE() --> it returns current time
    
    this differs from now()
    it returns the time at which it executes and
    now() returns time at which it began to execute
    
*/

SELECT SYSDATE()