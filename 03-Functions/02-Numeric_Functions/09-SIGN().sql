/*
  SIGN(x) --> it tells that the x is -ve, 0 or +ve
  
  if x<0 then it returns -1
  if x=0 then it returns 0 
  if x>0 then it returns 1 

*/

select sign(1);
select sign(1345);
select sign(334451);
select sign(01931);

select sign(0);

select sign(-1);
select sign(-1345);
select sign(-334451);
select sign(-01931);
