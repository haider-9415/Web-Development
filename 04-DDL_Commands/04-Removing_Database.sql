/*
	DROP DATABASE database_name --> to remove a database
    
    before removing a databse, make sure that you don't need data stored in
    different tablees of the database, because, all its tables also get removed
    
    if the database does not exist then it showws an error
*/

DROP DATABASE 12sixthdb;

DROP DATABASE fifth12db;

DROP DATABASE forth$db;

DROP DATABASE seconddb;

DROP DATABASE firstdb;

DROP DATABASE third_db;

-- all of the above databases have been removed that we created in 'CreatingDB' file 
